```mermaid

sequenceDiagram
    participant browser
    participant server

    Note over browser,server: Adding a New Note in SPA

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa

    activate server
    server-->>browser: JSON with new note added
    Note right of server: Server saves the note in database and returns the new note as JSON
    deactivate server

    Note right of browser: JS callback receives new note
    Note right of browser: Browser updates the notes list dynamically without reloading the page
